#include <iostream>

int main() 
{
	//¿Por qué nunca debería hacer lo siguiente?
	//por que se estan declarando variables con valores o numeros magicos.
	int y{ foo(++x, x) };
	double x{ 0.1 + 0.1 + 0.1 }; return (x == 0.3);
	int x {3/0};
	return 0;
}
